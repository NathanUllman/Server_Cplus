//std::string message = "Please Enter Password: ";
//send(newClient, message.c_str(), message.size() + 1, 0); // send a welcome message

#include "pch.h"
#include "Server.h"

void StateManager::MessageRecieved(User* user, std::string message) {
	
	

}